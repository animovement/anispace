% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quaternion_euler.R
\name{quat_from_euler}
\alias{quat_from_euler}
\alias{quat_to_euler}
\title{Quaternions from and to Euler angles}
\usage{
quat_from_euler(angles, sequence, intrinsic)

quat_to_euler(q, sequence, intrinsic)
}
\arguments{
\item{angles}{Angles in radians: a vector of length 3, or three columns,
in \code{sequence} order.}

\item{sequence}{Three axes, e.g. \code{"ZYX"}. Case is ignored.}

\item{intrinsic}{\code{TRUE} for rotations about the moving body axes, \code{FALSE}
for rotations about the fixed axes.}

\item{q}{Quaternions; see \link{quaternions}.}
}
\value{
\code{quat_from_euler()}: a quaternion matrix. \code{quat_to_euler()}: a
matrix with three columns in \code{sequence} order. The middle angle is in
\verb{[-pi/2, pi/2]} for Tait-Bryan sequences and \verb{[0, pi]} for proper ones;
the others are in \verb{(-pi, pi]}. At gimbal lock the third angle is 0.
}
\description{
Three rotations about coordinate axes, applied in \code{sequence}. There are
twelve sequences: six Tait-Bryan (\code{"ZYX"}, \code{"XYZ"}, …, all three axes) and
six proper Euler (\code{"ZXZ"}, \code{"XYX"}, …, first and last axis the same).
\itemize{
\item \strong{Intrinsic} rotations are about the body's own axes, which move with
each rotation. Intrinsic \code{"ZYX"} is yaw, pitch, roll.
\item \strong{Extrinsic} rotations are about the fixed frame's axes. An intrinsic
sequence equals the reversed extrinsic sequence with the angles reversed.
}

Neither \code{sequence} nor \code{intrinsic} has a default: an assumed convention is
the classic source of Euler-angle bugs, so the call must say which is
meant. Euler angles are a report format; store orientation as quaternions.
}
\examples{
ypr <- c(pi / 2, 0.1, -0.2)
q <- quat_from_euler(ypr, sequence = "ZYX", intrinsic = TRUE)
quat_to_euler(q, sequence = "ZYX", intrinsic = TRUE)

# The same rotation, described extrinsically
quat_to_euler(q, sequence = "XYZ", intrinsic = FALSE)
}
