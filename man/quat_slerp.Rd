% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quaternion_interp.R
\name{quat_slerp}
\alias{quat_slerp}
\alias{quat_mean}
\alias{quat_continuous}
\alias{quat_angular_velocity}
\title{Interpolate, average and differentiate rotations}
\usage{
quat_slerp(p, q, t)

quat_mean(q, weights = NULL)

quat_continuous(q)

quat_angular_velocity(q, dt, frame = c("fixed", "body"))
}
\arguments{
\item{p, q}{Quaternions; see \link{quaternions}.}

\item{t}{Interpolation fraction(s), recycled against the rows.}

\item{weights}{Optional non-negative weights, one per row.}

\item{dt}{Time between rows: one value, or one per row.}

\item{frame}{\code{"fixed"} or \code{"body"}.}
}
\value{
A quaternion matrix; \code{quat_mean()} returns one row;
\code{quat_angular_velocity()} returns a matrix with columns \code{x}, \code{y}, \code{z}.
}
\description{
\itemize{
\item \code{quat_slerp(p, q, t)}: spherical linear interpolation, \code{t = 0} giving \code{p}
and \code{t = 1} giving \code{q}, along the shorter arc.
\item \code{quat_mean(q, weights)}: the average rotation, as the eigenvector of the
largest eigenvalue of \verb{sum(w q q')} (Markley et al. 2007). Unlike
averaging components, it is unaffected by the sign ambiguity.
\item \code{quat_continuous(q)}: flips signs so each row lies on the same side as
the previous one. \code{q} and \code{-q} are the same rotation, so this changes no
rotation, but smoothing or differentiating a series without it goes
wrong wherever the sign jumps.
\item \code{quat_angular_velocity(q, dt)}: angular velocity between successive
rows, in radians per unit of \code{dt}, about the fixed frame's axes
(\code{frame = "fixed"}) or the body's own (\code{frame = "body"}). The first row
is \code{NA}.
}

Rows with \code{NA} are skipped by \code{quat_mean()} and carried through by the
others.
}
\examples{
a <- quat_from_axis_angle(c(0, 0, 1), 0)
b <- quat_from_axis_angle(c(0, 0, 1), pi / 2)
quat_slerp(a, b, 0.5)
quat_mean(rbind(a, b))

spin <- quat_from_axis_angle(c(0, 0, 1), seq(0, 1, by = 0.1))
quat_angular_velocity(spin, dt = 1 / 30)
}
