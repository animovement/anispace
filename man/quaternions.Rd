% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quaternion.R
\name{quaternions}
\alias{quaternions}
\alias{quat_multiply}
\alias{quat_conjugate}
\alias{quat_normalise}
\alias{quat_rotate}
\alias{quat_distance}
\title{Quaternion algebra}
\usage{
quat_multiply(p, q)

quat_conjugate(q)

quat_normalise(q)

quat_rotate(q, v)

quat_distance(p, q)
}
\arguments{
\item{p, q}{Quaternions.}

\item{v}{3D vectors.}
}
\value{
A matrix with columns \code{w}, \code{x}, \code{y}, \code{z} (or \code{x}, \code{y}, \code{z} for
\code{quat_rotate()}), or a numeric vector for \code{quat_distance()}.
}
\description{
Row-wise operations on unit quaternions. A quaternion argument is a numeric
vector of length 4, or a matrix or data frame with four columns, in the
order \code{w}, \code{x}, \code{y}, \code{z} (Hamilton convention, scalar first). Columns named
\code{w}/\code{x}/\code{y}/\code{z} or \code{qw}/\code{qx}/\code{qy}/\code{qz} are matched by name. A single row is
recycled against the other argument.
\itemize{
\item \code{quat_multiply(p, q)}: the Hamilton product \verb{p q}, the rotation \code{q}
followed by \code{p}.
\item \code{quat_conjugate(q)}: the inverse of a unit quaternion.
\item \code{quat_normalise(q)}: scaled to unit norm; a zero quaternion gives \code{NA}.
\item \code{quat_rotate(q, v)}: rotates 3D vectors \code{v} (length 3, or 3 columns).
\item \code{quat_distance(p, q)}: the angle of the rotation between \code{p} and \code{q}, in
\verb{[0, pi]}. \code{q} and \code{-q} are the same rotation, so their distance is 0.
}
}
\examples{
quarter_z <- quat_from_axis_angle(c(0, 0, 1), pi / 2)
quat_rotate(quarter_z, c(1, 0, 0))
quat_multiply(quarter_z, quarter_z)
quat_distance(quarter_z, quat_conjugate(quarter_z))

}
\seealso{
\code{\link[=quat_from_euler]{quat_from_euler()}}, \code{\link[=quat_slerp]{quat_slerp()}}
}
