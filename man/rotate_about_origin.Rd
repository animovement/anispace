% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform_coords_rotate.R
\name{rotate_about_origin}
\alias{rotate_about_origin}
\title{Rotate every subject's coordinates about the origin}
\usage{
rotate_about_origin(data, axes, align, level, align_perpendicular = FALSE)
}
\arguments{
\item{data}{An aniframe.}

\item{axes}{Named character vector, axis role to column.}

\item{align}{Values of \code{level} defining the axes.}

\item{level}{The identity variable they belong to.}

\item{align_perpendicular}{Put the primary axis across the target.}
}
\value{
\code{data}, rotated.
}
\description{
The rotation is worked out per group -- everything the frame is identified
and positioned by, except the level the alignment points belong to -- so
each subject at each moment gets its own. Reading the grouping from the
frame rather than assuming \code{individual} and \code{time} is what stops a second
trial's angle being applied to the first's rows (#20).
}
\keyword{internal}
