% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform_orientation.R
\name{transform_euler_to_quaternion}
\alias{transform_euler_to_quaternion}
\alias{transform_quaternion_to_euler}
\title{Convert orientation between Euler angles and quaternions}
\usage{
transform_euler_to_quaternion(
  data,
  euler,
  sequence,
  intrinsic,
  names = c("qw", "qx", "qy", "qz")
)

transform_quaternion_to_euler(
  data,
  sequence = NULL,
  intrinsic = NULL,
  names = c("euler_1", "euler_2", "euler_3")
)
}
\arguments{
\item{data}{A 3D anipoint.}

\item{euler}{The three Euler angle columns, in \code{sequence} order.}

\item{sequence, intrinsic}{The Euler convention; see \code{\link[=quat_from_euler]{quat_from_euler()}}.
For \code{transform_quaternion_to_euler()}, \code{NULL} uses the convention the
frame recorded.}

\item{names}{Names for the new columns.}
}
\value{
\code{data} with the new columns; for \code{transform_euler_to_quaternion()},
with \code{where$orientation} declared.
}
\description{
Trackers such as FicTrac and motion-capture software export orientation as
three Euler angles, but an anipoint stores 3D orientation as a unit
quaternion (animovement/anicore#46), which has no gimbal lock and no
wraparound. These functions are where the two meet, and where the Euler
convention is stated once.
\itemize{
\item \code{transform_euler_to_quaternion()} adds quaternion columns computed from
the Euler columns, declares them as the frame's orientation, and records
the convention (\code{euler_sequence}, \code{euler_intrinsic}).
\item \code{transform_quaternion_to_euler()} adds Euler columns computed from the
declared orientation, as a derived view for reporting or plotting. It
uses the recorded convention unless another is given.
}

Angles are read and written in the frame's \code{unit_angle}.
}
\examples{
df <- data.frame(
  time = 1:3, x = 0, y = 0, z = 0,
  yaw = c(0, 0.1, 0.2), pitch = 0, roll = c(0, 0, 0.1)
)
af <- anicore::as_anipoint(df) |>
  transform_euler_to_quaternion(
    c("yaw", "pitch", "roll"),
    sequence = "ZYX",
    intrinsic = TRUE
  )
anicore::get_variables(af, "where", "orientation")
}
