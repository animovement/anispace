# The rotation for one subject at one moment

The rotation for one subject at one moment

## Usage

``` r
rotation_for(primary, secondary, target)
```

## Arguments

- primary:

  The vector between the first two alignment points.

- secondary:

  The vector to the third, or `NULL`.

- target:

  Where they should end up.

## Value

A 3x3 rotation matrix.
